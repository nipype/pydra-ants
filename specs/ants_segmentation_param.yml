Atropos:
  output_requirements:

  output_templates:
    out_classified_image_name: ...
  doctest:
    # fixed_image: test.nii.gz
    # moving_image: test.nii.gz
    # --output [ output_, output_warped_image.nii.gz ] --metric Mattes[ test.nii, test.nii, 1, 32, Random, 0.05 ]
    cmdline: >-
      atropos
  tests_inputs:
    -
  tests_outputs:
    # - AttributeError


N4BiasFieldCorrection:
  output_requirements:
    output_image: [input_image]

  output_templates:
    output_image: "{input_image}_corrected"

  doctest:
    input_image: "test.nii.gz"
    output_image: test_corrected.nii.gz
    cmdline: 'N4BiasFieldCorrection --input-image test.nii.gz --output test_corrected.nii.gz'

  tests_inputs:
    -

  tests_outputs:
    - AttributeError
